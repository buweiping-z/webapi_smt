# 点检系统功能扩展 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为设备点检管理系统追加点检资格人员表、必须/选择点检标志、点检者行显示、工号验证等5项功能，并修复 `SaveDailyRecord` 预存 Bug。

**Architecture:** 遵循现有3层架构（Models → Data → Controllers）。新增 `QualifiedInspector` Model 映射新表，在 `InspectionController` 中追加5个端点并修改3个端点。前端在单一 `index.html` 中追加点检者下拉框和点检者行。

**Tech Stack:** .NET 8.0, ASP.NET Core Web API, EF Core 8.0, MySQL (Pomelo), 纯 JS 前端

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `Models/QualifiedInspector.cs` | 新增 | 点检资格人员实体，映射 `qualified_inspectors` 表 |
| `Models/InspectionTemplate.cs` | 修改 | 新增 `IsMandatory` 属性 |
| `Data/AppDbContext.cs` | 修改 | 注册 `QualifiedInspector` DbSet + Fluent 配置 |
| `Controllers/InspectionController.cs` | 修改 | 5个新端点 + 3个修改端点 + 请求模型变更 |
| `html/index.html` | 修改 | 文本变更 + 点检者下拉框 + 点检者行 + 提交验证 + API 调用 |

---

### Task 1: 数据库迁移

**Files:**
- 生成: EF Core Migration 文件（自动生成）

- [ ] **Step 1: 创建 EF Core Migration**

```bash
dotnet ef migrations add AddQualifiedInspectorsAndIsMandatory
```

- [ ] **Step 2: 应用迁移到数据库**

```bash
dotnet ef database update
```

- [ ] **Step 3: 验证数据库变更**

连接 MySQL 验证：

```sql
-- 验证新表存在
SHOW TABLES LIKE 'qualified_inspectors';

-- 验证新列存在
SHOW COLUMNS FROM inspection_templates LIKE 'is_mandatory';
```

---

### Task 2: Model 层变更

**Files:**
- Create: `Models/QualifiedInspector.cs`
- Modify: `Models/InspectionTemplate.cs`

- [ ] **Step 1: 创建 `Models/QualifiedInspector.cs`**

```csharp
using System.ComponentModel.DataAnnotations.Schema;

namespace webapi.Models
{
    [Table("qualified_inspectors")]
    public class QualifiedInspector
    {
        [Column("id")]
        public int Id { get; set; }

        [Column("employee_id")]
        public string EmployeeId { get; set; } = string.Empty;

        [Column("last_name")]
        public string LastName { get; set; } = string.Empty;
    }
}
```

- [ ] **Step 2: 修改 `Models/InspectionTemplate.cs`** — 在 `SortOrder` 属性之后、类结束 `}` 之前添加

在文件第30行 `public int SortOrder { get; set; }` 之后追加：

```csharp
        [Column("is_mandatory")]
        public bool IsMandatory { get; set; } = true;
```

- [ ] **Step 3: 验证编译**

```bash
dotnet build
```

预期: Build succeeded.

---

### Task 3: AppDbContext 注册

**Files:**
- Modify: `Data/AppDbContext.cs`

- [ ] **Step 1: 添加 DbSet 属性**

在 `Data/AppDbContext.cs` 第15行 `public DbSet<InspectionUser> InspectionUsers { get; set; }` 之后追加：

```csharp
        public DbSet<QualifiedInspector> QualifiedInspectors { get; set; }
```

- [ ] **Step 2: 添加 Fluent 配置**

在 `OnModelCreating` 方法的 `InspectionUser` 配置块之后（约第68行 `});` 之后）追加：

```csharp
            modelBuilder.Entity<QualifiedInspector>(entity =>
            {
                entity.ToTable("qualified_inspectors");
                entity.HasKey(e => e.Id);
                entity.Property(e => e.Id).HasColumnName("id");
                entity.Property(e => e.EmployeeId).HasColumnName("employee_id").HasMaxLength(9).IsRequired();
                entity.Property(e => e.LastName).HasColumnName("last_name").HasMaxLength(6).IsRequired();
                entity.HasIndex(e => e.EmployeeId)
                    .IsUnique()
                    .HasDatabaseName("idx_qualified_inspectors_employee_id");
            });
```

- [ ] **Step 3: 验证编译**

```bash
dotnet build
```

预期: Build succeeded.

---

### Task 4: API — Operators CRUD 端点

**Files:**
- Modify: `Controllers/InspectionController.cs`

- [ ] **Step 1: 添加 `GET api/inspection/operators/list` 端点**

在 `InspectionController` 类中（`AutoSign` 方法之后，约第414行 `}` 之后）添加：

```csharp
        // 14. 获取点检资格人员列表
        [HttpGet("operators/list")]
        public async Task<IActionResult> GetOperatorList()
        {
            var operators = await _context.QualifiedInspectors
                .Select(o => new { o.EmployeeId, o.LastName })
                .ToListAsync();

            return Ok(operators);
        }
```

- [ ] **Step 2: 添加 `POST api/inspection/operators/add` 端点**

继续追加：

```csharp
        // 15. 新增点检资格人员（支持单个/批量）
        [HttpPost("operators/add")]
        public async Task<IActionResult> AddOperators([FromBody] List<AddOperatorRequest> requests)
        {
            if (requests == null || requests.Count == 0)
                return BadRequest(new { success = false, message = "请求数据不能为空" });

            var existingIds = await _context.QualifiedInspectors
                .Select(o => o.EmployeeId)
                .ToListAsync();

            var existingSet = new HashSet<string>(existingIds);
            var added = 0;

            foreach (var req in requests)
            {
                if (string.IsNullOrEmpty(req.EmployeeId) || string.IsNullOrEmpty(req.LastName))
                    continue;

                if (existingSet.Contains(req.EmployeeId))
                    return BadRequest(new { success = false, message = $"工号 {req.EmployeeId} 已存在" });

                _context.QualifiedInspectors.Add(new QualifiedInspector
                {
                    EmployeeId = req.EmployeeId,
                    LastName = req.LastName
                });
                existingSet.Add(req.EmployeeId);
                added++;
            }

            await _context.SaveChangesAsync();
            return Ok(new { success = true, message = $"已添加 {added} 名点检资格人员" });
        }
```

- [ ] **Step 3: 添加 `DELETE api/inspection/operators/{employeeId}` 端点**

继续追加：

```csharp
        // 16. 移除点检资格人员
        [HttpDelete("operators/{employeeId}")]
        public async Task<IActionResult> DeleteOperator(string employeeId)
        {
            var operator_ = await _context.QualifiedInspectors
                .FirstOrDefaultAsync(o => o.EmployeeId == employeeId);

            if (operator_ == null)
                return NotFound(new { success = false, message = $"工号 {employeeId} 不存在" });

            _context.QualifiedInspectors.Remove(operator_);
            await _context.SaveChangesAsync();

            return Ok(new { success = true, message = $"已移除点检资格人员 {employeeId}" });
        }
```

- [ ] **Step 4: 在文件末尾添加请求模型 `AddOperatorRequest`**

在文件末尾（`AutoSignRequest` 类之后，约第808行 `}` 之后）追加：

```csharp
    public class AddOperatorRequest
    {
        public string EmployeeId { get; set; } = string.Empty;
        public string LastName { get; set; } = string.Empty;
    }
```

- [ ] **Step 5: 验证编译**

```bash
dotnet build
```

预期: Build succeeded.

---

### Task 5: API — Operators Validate + Daily 端点

**Files:**
- Modify: `Controllers/InspectionController.cs`

- [ ] **Step 1: 添加 `GET api/inspection/operators/validate/{employeeId}` 端点**

在 `DeleteOperator` 方法之后追加：

```csharp
        // 17. 验证工号是否为点检资格人员
        [HttpGet("operators/validate/{employeeId}")]
        public async Task<IActionResult> ValidateOperator(string employeeId)
        {
            var operator_ = await _context.QualifiedInspectors
                .FirstOrDefaultAsync(o => o.EmployeeId == employeeId);

            if (operator_ == null)
                return Ok(new { valid = false });

            return Ok(new
            {
                valid = true,
                employeeId = operator_.EmployeeId,
                lastName = operator_.LastName
            });
        }
```

- [ ] **Step 2: 添加 `GET api/inspection/operators/daily` 端点**

继续追加：

```csharp
        // 18. 获取指定设备+年月的每日点检者姓
        [HttpGet("operators/daily")]
        public async Task<IActionResult> GetDailyOperators(string deviceModel, int year, int month)
        {
            var startDate = new DateTime(year, month, 1);
            var endDate = startDate.AddMonths(1).AddDays(-1);

            // 查询该设备当月所有记录，按日分组取最新记录的 employee_id
            var records = await _context.InspectionRecords
                .Where(r => r.DeviceModel == deviceModel
                    && r.InspectionTime >= startDate
                    && r.InspectionTime <= endDate)
                .ToListAsync();

            // 按日分组，取每天最新记录
            var dailyLatest = records
                .GroupBy(r => r.InspectionTime.Day)
                .ToDictionary(
                    g => g.Key,
                    g => g.OrderByDescending(r => r.InspectionTime).First().EmployeeId
                );

            // JOIN qualified_inspectors 获取姓
            var employeeIds = dailyLatest.Values.Distinct().ToList();
            var inspectors = await _context.QualifiedInspectors
                .Where(o => employeeIds.Contains(o.EmployeeId))
                .ToDictionaryAsync(o => o.EmployeeId, o => o.LastName);

            var result = new Dictionary<string, string>();
            foreach (var kvp in dailyLatest)
            {
                result[kvp.Key.ToString()] = inspectors.TryGetValue(kvp.Value, out var name) ? name : "";
            }

            return Ok(new
            {
                deviceModel,
                year,
                month,
                inspectors = result
            });
        }
```

- [ ] **Step 3: 验证编译**

```bash
dotnet build
```

预期: Build succeeded.

---

### Task 6: API — 修改 submit-full 和 submit 端点（加工号验证）

**Files:**
- Modify: `Controllers/InspectionController.cs`

- [ ] **Step 1: 修改 `POST api/inspection/submit-full`** — 在方法体开头添加验证

在 `SubmitFullInspection` 方法中，现有 `if (request == null ...` 验证之后、`var now = DateTime.Now;` 之前（约第67行），追加工号验证：

```csharp
            // 验证工号是否为点检资格人员
            var isQualified = await _context.QualifiedInspectors
                .AnyAsync(o => o.EmployeeId == request.EmployeeId);
            if (!isQualified)
                return BadRequest(new { success = false, message = "该工号无点检资格" });
```

- [ ] **Step 2: 修改 `POST api/inspection/submit`（旧端点）** — 同样追加验证

在 `SubmitInspection` 方法中，现有 `if (request == null ...` 验证之后、`var record = new ...` 之前（约第28行），追加：

```csharp
            // 验证工号是否为点检资格人员
            var isQualified = await _context.QualifiedInspectors
                .AnyAsync(o => o.EmployeeId == request.EmployeeId);
            if (!isQualified)
                return BadRequest(new { success = false, message = "该工号无点检资格" });
```

- [ ] **Step 3: 验证编译**

```bash
dotnet build
```

预期: Build succeeded.

---

### Task 7: API — 修复 records/save + 修改 frequency-summary

**Files:**
- Modify: `Controllers/InspectionController.cs`

- [ ] **Step 1: 修复 `POST api/inspection/records/save`** — 修复创建新记录时未设置 Frequency/PeriodKey 的 Bug

在 `SaveDailyRecord` 方法中，找到创建新记录的代码块（约第227-238行），将：

```csharp
                        var newRecord = new InspectionRecord
                        {
                            EmployeeId = "web",
                            DeviceModel = request.DeviceModel,
                            DeviceName = request.DeviceModel,
                            InspectionTime = new DateTime(inspectionDate.Year, inspectionDate.Month, item.Day),
                            Status = "submitted",
                            ResultsJson = ""
                        };
```

替换为：

```csharp
                        var newRecord = new InspectionRecord
                        {
                            EmployeeId = string.IsNullOrEmpty(request.EmployeeId) ? "web" : request.EmployeeId,
                            DeviceModel = request.DeviceModel,
                            DeviceName = request.DeviceModel,
                            InspectionTime = new DateTime(inspectionDate.Year, inspectionDate.Month, item.Day),
                            Status = "submitted",
                            ResultsJson = "",
                            Frequency = frequency,
                            PeriodKey = periodKey
                        };
```

- [ ] **Step 2: 修改 `SaveDailyRecordRequest`** — 追加 `EmployeeId` 字段

找到文件末尾的 `SaveDailyRecordRequest` 类（约第771行），将：

```csharp
    public class SaveDailyRecordRequest
    {
        public string DeviceModel { get; set; } = string.Empty;
        public DateTime InspectionMonth { get; set; }
        public List<SaveRecordItem> Results { get; set; } = new();
    }
```

替换为：

```csharp
    public class SaveDailyRecordRequest
    {
        public string EmployeeId { get; set; } = string.Empty;
        public string DeviceModel { get; set; } = string.Empty;
        public DateTime InspectionMonth { get; set; }
        public List<SaveRecordItem> Results { get; set; } = new();
    }
```

- [ ] **Step 3: 修改 `GET api/inspection/frequency-summary`** — 按必须点检项目统计漏检

在 `BuildFrequencyStats` 方法中（约第571行），修改统计逻辑。将方法体中的设备遍历逻辑替换为：

找到方法中 `foreach (var device in devices)` 循环体（约第586-634行），将整个 `foreach` 块替换为：

```csharp
            foreach (var device in devices)
            {
                // 获取该设备在该频率下的所有必须点检项目
                var mandatoryItems = await _context.InspectionTemplates
                    .Where(t => t.DeviceModel == device
                        && t.Frequency == frequency
                        && t.IsMandatory)
                    .Select(t => t.ItemName)
                    .ToListAsync();

                // 该设备在该频率下无必须点检项目 → 跳过
                if (mandatoryItems.Count == 0)
                    continue;

                // 获取该设备本月内该频率的所有记录及结果
                var records = await _context.InspectionRecords
                    .Where(r => r.DeviceModel == device
                        && r.Frequency == frequency
                        && r.InspectionTime >= startDate
                        && r.InspectionTime <= endDate)
                    .ToListAsync();

                if (records.Count == 0)
                {
                    uninspectedDevices.Add(device);
                    continue;
                }

                // 收集所有记录的 InspectionResult item_name
                var recordIds = records.Select(r => r.Id).ToList();
                var resultItemNames = await _context.InspectionResults
                    .Where(res => recordIds.Contains(res.RecordId))
                    .Select(res => res.ItemName)
                    .Distinct()
                    .ToListAsync();

                var resultSet = new HashSet<string>(resultItemNames);

                // 检查是否所有必须点检项目都有结果
                var allMandatoryInspected = mandatoryItems.All(item => resultSet.Contains(item));

                if (allMandatoryInspected)
                    inspectedCount++;
                else
                {
                    abnormalCount++;
                    abnormalDeviceModels.Add(device);
                }
            }
```

- [ ] **Step 4: 验证编译**

```bash
dotnet build
```

预期: Build succeeded.

---

### Task 8: 前端 — 文本变更 + 点检者下拉框

**Files:**
- Modify: `html/index.html`

- [ ] **Step 1: 修改 "检查" → "检查/清扫"**

在 `renderReadOnlyTable()` 函数中，找到第1240行：

```javascript
                let contentText = '检查';
```

替换为：

```javascript
                let contentText = '检查/清扫';
```

- [ ] **Step 2: 添加全局变量 `currentInspectorId`**

在 `<script>` 标签内，与其他全局变量（第777-789行区域）一起，在 `let currentUser = null;` 之后添加：

```javascript
        let currentInspectorId = null;  // 当前选中的点检者工号
```

- [ ] **Step 3: 在用户信息栏添加点检者下拉框**

在 HTML 中，找到 `userBar` div（约第664行），修改为：

```html
        <div id="userBar" class="user-bar" style="display:none;">
            <span id="userInfo"></span>
            <span id="inspectorSpan" style="display:none; margin-left: 20px;">
                点检者：
                <select id="inspectorSelect" style="padding: 4px 8px; border-radius: 4px; border: 1px solid #ddd;" onchange="onInspectorChange()">
                    <option value="">选择点检者</option>
                </select>
            </span>
            <button class="logout-btn" onclick="logout()">退出登录</button>
        </div>
```

- [ ] **Step 4: 添加 `loadInspectorList()` 函数**

在 `<script>` 标签内添加新函数：

```javascript
        // 加载点检资格人员列表
        async function loadInspectorList() {
            try {
                const response = await axios.get(`${API_BASE_URL}/api/Inspection/operators/list`);
                const select = document.getElementById('inspectorSelect');
                // 保留第一个"选择点检者"选项
                select.innerHTML = '<option value="">选择点检者</option>';
                response.data.forEach(op => {
                    const option = document.createElement('option');
                    option.value = op.employeeId;
                    option.textContent = `${op.lastName} (${op.employeeId})`;
                    select.appendChild(option);
                });
            } catch (error) {
                console.error('加载点检者列表失败:', error);
            }
        }

        function onInspectorChange() {
            const select = document.getElementById('inspectorSelect');
            currentInspectorId = select.value || null;
        }
```

- [ ] **Step 5: 修改 `login()` 函数** — 登录后加载点检者列表

在 `login()` 函数的成功分支中，`document.getElementById('userBar').style.display = 'flex';` 之后添加：

```javascript
                    // 如果是担当角色，显示点检者选择框
                    if (currentUser.role === 'operator') {
                        document.getElementById('inspectorSpan').style.display = 'inline';
                        await loadInspectorList();
                    }
```

- [ ] **Step 6: 修改 `logout()` 函数** — 清除点检者状态

在 `logout()` 函数中，`currentUser = null;` 之后添加：

```javascript
            currentInspectorId = null;
            document.getElementById('inspectorSpan').style.display = 'none';
```

---

### Task 9: 前端 — 点检者行 + 提交验证

**Files:**
- Modify: `html/index.html`

- [ ] **Step 1: 添加 `loadDailyInspectors()` API 调用**

在 `<script>` 标签内添加新函数（放在 `loadRecords` 函数附近）：

```javascript
        // 加载每日点检者映射
        async function loadDailyInspectors(deviceModel, year, month) {
            try {
                const response = await axios.get(`${API_BASE_URL}/api/Inspection/operators/daily`, {
                    params: { deviceModel, year, month }
                });
                return response.data.inspectors || {};
            } catch (error) {
                console.error('加载每日点检者失败:', error);
                return {};
            }
        }
```

- [ ] **Step 2: 修改 `renderReadOnlyTable()`** — 添加点检者行

将 `renderReadOnlyTable` 改为 `async function renderReadOnlyTable()`，并在 `html += '</tbody>';` 之前追加点检者行。

具体修改：

**步骤2a**: 将函数签名从：
```javascript
        function renderReadOnlyTable() {
```
改为：
```javascript
        async function renderReadOnlyTable() {
```

**步骤2b**: 在函数开头（`const daysInMonth = ...` 之后，`const recordMap = new Map();` 之前）添加 API 调用：

```javascript
            // 加载每日点检者
            const dailyInspectors = await loadDailyInspectors(currentDevice, currentYear, currentMonth);
```

**步骤2c**: 在 `html += '</tbody>';`（约第1302行）之前追加点检者行：

```javascript
            // 点检者行
            html += '<tr style="background:#f0f4ff; font-weight:bold;">';
            html += '<td colspan="3" style="text-align:center; background:#e8edf5; font-weight:bold;">点检者</td>';
            html += '<td style="background:#e8edf5;"></td>';
            for (let day = 1; day <= daysInMonth; day++) {
                const name = dailyInspectors[String(day)] || '';
                html += `<td style="background:#f0f4ff;">${name}</td>`;
            }
            html += '<td style="background:#e8edf5;"></td>';
            html += '</tr>';
```

- [ ] **Step 3: 修改 `loadData()`** — 调用改为 await renderReadOnlyTable

找到 `loadData()` 函数中的 `renderReadOnlyTable();` 调用（约第1341行），改为：

```javascript
                await renderReadOnlyTable();
```

- [ ] **Step 4: 修改 `batchExportAll()` 中的 `renderReadOnlyTable()` 调用**

找到 `batchExportAll()` 函数中的 `renderReadOnlyTable();`（约第1603行），改为：

```javascript
                    await renderReadOnlyTable();
```

恢复上下文处的 `renderReadOnlyTable();`（约第1618行）也改为：

```javascript
                await renderReadOnlyTable();
```

- [ ] **Step 5: 前端提交验证** — 如果没有点检者则阻止保存

系统使用 `records/save` 保存数据。在 HTTP 请求附近（如果存在前端保存逻辑）添加验证。检查现有代码... 当前 `index.html` 没有调用 `records/save` 的前端代码（该端点存在但似乎由其他客户端调用）。如果未来添加前端保存功能，需在提交前调用 `validate` API。

对于手机端调用 `submit-full` 的场景，在调用前验证：

```javascript
// 在调用 submit-full 之前
if (employeeId) {
    const resp = await axios.get(`${API_BASE_URL}/api/Inspection/operators/validate/${employeeId}`);
    if (!resp.data.valid) {
        alert('该工号无点检资格');
        return;
    }
}
```

---

### Task 10: 端到端验证

- [ ] **Step 1: 构建并运行项目**

```bash
dotnet build
dotnet run
```

预期: 项目在 `http://localhost:5039` 启动。

- [ ] **Step 2: 验证点检资格人员 CRUD API**

```bash
# 添加点检资格人员
curl -X POST http://localhost:5039/api/inspection/operators/add \
  -H "Content-Type: application/json" \
  -d '[{"employeeId":"SN000001","lastName":"张"},{"employeeId":"SN000002","lastName":"李"}]'

# 预期: {"success":true,"message":"已添加 2 名点检资格人员"}

# 获取列表
curl http://localhost:5039/api/inspection/operators/list

# 预期: [{"employeeId":"SN000001","lastName":"张"},{"employeeId":"SN000002","lastName":"李"}]

# 验证工号
curl http://localhost:5039/api/inspection/operators/validate/SN000001

# 预期: {"valid":true,"employeeId":"SN000001","lastName":"张"}

# 验证无效工号
curl http://localhost:5039/api/inspection/operators/validate/SN999999

# 预期: {"valid":false}

# 删除工号
curl -X DELETE http://localhost:5039/api/inspection/operators/SN000002

# 预期: {"success":true,"message":"已移除点检资格人员 SN000002"}
```

- [ ] **Step 3: 验证 submit-full 工号拒绝**

```bash
curl -X POST http://localhost:5039/api/inspection/submit-full \
  -H "Content-Type: application/json" \
  -d '{"employeeId":"SN999999","deviceModel":"TestDevice","results":[]}'

# 预期: 400 Bad Request, {"success":false,"message":"该工号无点检资格"}
```

- [ ] **Step 4: 验证 daily API**

```bash
curl "http://localhost:5039/api/inspection/operators/daily?deviceModel=TestDevice&year=2026&month=6"

# 预期: {"deviceModel":"TestDevice","year":2026,"month":6,"inspectors":{...}}
```

- [ ] **Step 5: 验证 frequency-summary 新逻辑**

```bash
curl "http://localhost:5039/api/inspection/frequency-summary?year=2026&month=6"

# 预期: {"daily":{...},"weekly":{...},"monthly":{...}}，统计口径为必须点检项目
```

- [ ] **Step 6: 验证前端显示**

浏览器访问 `http://localhost:8080`:
1. 登录（选择担当角色用户）
2. 确认出现"点检者"下拉框
3. 选择设备和月份，点击查询
4. 确认表格"内容"列显示"检查/清扫"
5. 确认表格底部出现"点检者"行
6. 确认日/周/月统计卡片正确显示

---

## 验证清单

| # | 验证项 | 方法 |
|---|--------|------|
| 1 | "检查" → "检查/清扫" | 浏览器查看表格内容列 |
| 2.1 | 资格人员表存在 | MySQL 查询 |
| 2.2 | 资格人员 CRUD API 正常 | curl 测试 |
| 3 | "点检者"行正确显示 | 浏览器查看表格底部 |
| 4.1 | is_mandatory 列存在 | MySQL 查询 |
| 4.2 | 漏检统计按必须点检口径 | curl frequency-summary |
| 5 | submit-full 拒绝无效工号 | curl 测试 |
| 6 | SaveDailyRecord 正确设置 PeriodKey | 检查数据库 inspection_records 表 |
